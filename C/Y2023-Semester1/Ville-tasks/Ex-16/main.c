#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>
#include <stdbool.h>

/* call this function to read input */
bool read_line(char *line, size_t size);

#include <stdio.h>

int main(void)
{
    printf

    return 0;
}

bool read_line(char *line, size_t size)
{
    return true;
}
